# partial yaml file to define feature generation tasks so we can reuse them between train and serving
  - source: src/features/create_tfidf_scores.py
    product:
      nb: output/notebooks/create_tfidf.ipynb
      data: output/data/features/tfidf_vector.csv
